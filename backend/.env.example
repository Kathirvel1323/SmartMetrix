# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# Replace with your MongoDB connection string (local MongoDB or MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/smartmetrix

# CORS Configuration
CORS_ORIGIN=*

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long
JWT_EXPIRES_IN=7d

# Initial Admin Bootstrap (Used by npm run seed:admin)
ADMIN_NAME=Super Admin
ADMIN_EMAIL=admin@smartmetrix.local
# Developer MUST set a strong, unique password (min 8 characters with letters, numbers, and special symbols)
ADMIN_PASSWORD=

# AI Service Integration (FastAPI Microservice)
AI_SERVICE_URL=http://localhost:8000
AI_SERVICE_TOKEN=your_ai_service_secret_token_min_32_chars
AI_SERVICE_TIMEOUT_MS=5000
